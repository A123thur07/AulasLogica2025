const prompt = require('prompt-sync')();
function Tabuada(n1){
    for(var x=1; x<=10;x++){
        console.log(n1*x);
    }
}

Tabuada(Number(prompt('Digite o número: ')))
Tabuada(Number(prompt('Digite o número: ')))
Tabuada(Number(prompt('Digite o número: ')))

 